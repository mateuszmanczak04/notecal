import { Metadata } from 'next';

export const metadata: Metadata = {
	title: 'Calendar view',
	robots: {
		index: false,
	},
};

// const page = () => <CalendarPage />;

const page = () => <p>TODO</p>;

export default page;
